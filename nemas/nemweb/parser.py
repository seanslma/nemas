import polars as pl


def extract_zip_csv(
    file: str,
) -> dict[str, pl.DataFrame]:
    """
    str_lens = rng.integers(
        low=str_len[0], high=str_len[1], size=str_cnt, endpoint=True
    )
    """
    pass
